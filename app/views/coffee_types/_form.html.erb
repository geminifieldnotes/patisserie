<%= form_with(model: coffee_type) do |form| %>
  <% if coffee_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(coffee_type.errors.count, "error") %> prohibited this coffee_type from being saved:</h2>

      <ul>
        <% coffee_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
